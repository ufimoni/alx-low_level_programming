// Here is a Funtion that checks for the uppercase character
// we are using the if and else statement
[200~int _isupper(int c) {
	    if (c >= 'A' && c <= 'Z') {
        return 1;  // will Indicate if it is  Uppercase character
    } else {
        return 0;  // Not an uppercase character
    }
}
