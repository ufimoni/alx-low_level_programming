// Here is a  a function that checks for a digit (0 through 9).
int _isdigit(int c) {
    if (c >= '0' && c <= '9') 
    {
        return 1;  // it checks if it is a Digit it returns 1
    } else {

        return 0;  // Not a digit or it returns 0.
    }
}
